En este momento ya tienes creada la versión web y PWA, la cual se utiliza desde el navegador que el usuario tenga, bien sea Safari, Google Chrome o Firebase, no me interesa cuál use. En este momento tienes el proyecto en página web. Ahora quiero que implementes todo para el APK con instrucciones, todo listo para que esté conectado y sincronizado, listo para compilar. Yo lo voy a compilar en GitHub Codespaces, así que debes implementar todo listo para exportar el proyecto de aquí hacia GitHub y en GitHub Codespaces con Copilot, darle la instrucción para que compile y lo único que él tenga que hacer sea seguir instrucciones, ya que no deba crear, optimizar ni nada, todo debe venir integrado acá. Por eso te digo, todo debe ser real y conexiones reales, remotas, sincronizadas, porque la APK, la PWA y la web deben estar sincronizadas con la misma información, ya que hay usuarios que ingresan desde la aplicación APK, otros desde la PWA y otros desde la web. Entonces, deben estar todos sincronizados en tiempo real con la misma información. mi token expo es este :       uBp0GHI0sEx2xZ2sPOqzDKk7EH6koqK0ea6wPIZs
la url publica remota de este proyecto es : bancolombia--appbancolombiav.replit.app
debes crear las bases de datos y exponer los datos reales para el apk , Deben ser los datos exactos. No quiero que me pongas ejemplos para que yo los edite a como yo los tengo, porque yo no sé cómo está configurado el proyecto ni qué tiene. Por eso te digo, implementa las bases de datos, conecta las conexiones, programa los permisos, configura para que la versión PWA pueda funcionar interfiriendo con el APK y el APK transmitiendo hacia la web y hacia él mismo, porque el APK debe contener la misma información. Entonces, si los usuarios del APK quieren ingresar a la web, pueden hacerlo. Y si los de la web quieren ingresar al APK, pueden instalar el APK e ingresar con las mismas credenciales. Y como puedes ver, hay permisos que solamente están implementados para Android versiones antiguas y otros permisos distintos para versiones nuevas. Entonces, necesito que el APK detecte qué versión de Android es el celular y muestre los permisos que corresponden a esa versión, ya que hay permisos de versiones, por lo menos ejemplo Android 9, que no están implementados en el Android 15. Quiero que el APK detecte y pida los permisos de manera mensaje "flash" emergente en la pantalla, esperando a permitir o no permitir de parte del usuario, pero debe mostrarse de manera "flash" y correspondiente a la versión. Quiero que implementes estos ajustes para la versión desde el Android 8 hasta la última versión de Android. Así que implementa los permisos que se requieran para cada versión y que el APK sepa qué debe hacer.